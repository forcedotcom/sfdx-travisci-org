({    
    
    submitForm : function(component, event, helper) {
        
        var validCamping = true;

       

        if(validCamping){
            
            helper.createItem(component);
            
        }
        
    },
})